import styled from 'styled-components';

export const StyledInput = styled.input({
  backgroundColor: '#d8d8d8',
  outline: 0,
  border: 0,
  padding: '8px',
  borderRadius: '5px',
  margin: '5px',
});
